# Ghost AI — GT7 Racing Intelligence
# Requirements — install with: pip install -r requirements.txt
# macOS also needs: brew install tesseract
 
# ── Core ML ───────────────────────────────────────────────
numpy>=1.24
scikit-learn>=1.3
pandas>=2.0
 
# ── Web dashboard ─────────────────────────────────────────
flask>=3.0
 
# ── GT7 live telemetry receiver ───────────────────────────
gt-telem>=0.2
 
# ── Video-based telemetry (analyze_video.py) ──────────────
opencv-python>=4.9
pytesseract>=0.3.10
 
# ── Database (storage/) ───────────────────────────────────
# SQLite ships with Python — no install needed for ghost.db
# Add SQLAlchemy only if you move beyond raw sqlite3
# sqlalchemy>=2.0
 
# ── API server (api/) ─────────────────────────────────────
# Flask covers this — no extra deps needed yet
 
# ── Dev / testing ─────────────────────────────────────────
# pytest>=8.0
 